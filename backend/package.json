{
  "name": "careband-backend",
  "version": "0.1.3",
  "private": true,
  "type": "module",
  "description": "CareBand backend skeleton — loopback-only Express health check.",
  "engines": {
    "node": ">=22.12.0 <23"
  },
  "scripts": {
    "dev": "node src/server.js",
    "start": "node src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.21.0"
  }
}
