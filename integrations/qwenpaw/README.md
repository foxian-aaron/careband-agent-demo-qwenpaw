# QwenPaw Integration

Run `scripts/configure-qwenpaw-agent.ps1` after QwenPaw is installed. The script expects the local agent id `careband_summary_agent`, disables all runtime tools, selects the existing Alibaba `qwen3.6-plus` provider, and installs the prompt files in this directory.

No API key or provider secret is stored in this repository.
